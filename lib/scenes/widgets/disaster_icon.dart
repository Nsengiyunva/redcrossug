import 'package:flutter/widgets.dart';

class DisasterIcon extends StatelessWidget {
  const DisasterIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}